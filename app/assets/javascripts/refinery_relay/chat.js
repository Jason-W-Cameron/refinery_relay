// Compatibility entry point for Rails 5 hosts that still include this logical
// asset from application.js.
//
// Relay Chat is loaded by the pod's configured widget markup. Keeping this
// asset present allows Sprockets 3 to compile the existing host manifest
// without loading the removed Action Cable client.
